% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_variables.R
\name{list_variables}
\alias{list_variables}
\title{List tilgjengelige variabler}
\usage{
list_variables(data = NULL, category = NULL, type = NULL, print = TRUE)
}
\arguments{
\item{data}{Et NorMacro-datasett eller `NULL`. Dersom `NULL`, brukes
den samlede NorMacro-metadataen.}

\item{category}{Valgfri kategori som variabellisten skal begrenses til.
Standard er `NULL`, som inkluderer alle kategorier.}

\item{type}{Valgfri variabeltype som listen skal begrenses til.
Standard er `NULL`, som inkluderer alle typer.}

\item{print}{Logisk verdi. Dersom `TRUE`, skrives en formatert oversikt
til konsollen. Standard er `TRUE`.}
}
\value{
Metadata for variablene som oppfyller kriteriene, returnert
  usynlig.
}
\description{
Gir en oversikt over variablene som er tilgjengelige i NorMacro eller
i et angitt datasett. Variablene grupperes etter kategori og vises med
både variabelnavn og beskrivende navn når dette finnes.
}
\details{
Listen kan avgrenses til en bestemt kategori eller variabeltype.
Funksjonen er særlig nyttig for å utforske hvilke data som finnes før
en analyse.
}
\examples{
\dontrun{
list_variables()

# Resultatet kan også lagres uten utskrift
variables <- list_variables(
  print = FALSE
)
}

}
